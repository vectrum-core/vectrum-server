const mongoose = require('mongoose');
const User = mongoose.model('users');
const TGUser = mongoose.model('tg_users');
const Language = mongoose.model('Language');
const logger = require('../../logger');



const log = logger.getLogger('BOT:DB');


module.exports = {
};
